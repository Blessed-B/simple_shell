#include <stdio.h>

/**
 * add_num - adds two integers
 * @a: iinteger to be added
 * @b: integer to be added
 * Return: sum of integers
 */

int add_num(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
